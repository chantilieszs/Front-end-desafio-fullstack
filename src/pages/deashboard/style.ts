import styled from 'styled-components'

export const DeashContainer = styled.section`
    height: 100vh;
    background-color: rgba(0, 84, 252, .4);
    ul {
        display: flex;
        width: 100%;
        flex-direction: row;
        list-style: none;
    }
`